# oruca — プロジェクトリファレンス

## プロジェクト概要

oruca は、大学やオフィスなどの特定エリアに入った時だけ位置情報を検知し、
それ以外の場所・時間のデータは保存・送信しない、エリア限定・関係限定の
在席可視化アプリ。詳細は `docs/prd.md` を参照。

コンペティブプログラムへの提出を目標に、2月まで開発を継続する。

## 現在のステータス（重要・最初に読むこと）

**合宿デモ用の緊急モック（`demo/`）は作成途中で不要と判明したため中止し、
リポジトリから削除した。** 現在は本開発（`app/`）に直接着手している段階。
モックデータに対して画面が動く状態を先に作り、バックエンド（Supabase・DDL・
RLS）は後から接続する方針で進める（詳細は各User StoryのGitHub Issue参照）。

## 開発者について（AIが常に踏まえるべき前提）

- モバイル開発は約1年だが、既存プロジェクトへの参加のみで、ゼロからの
  立ち上げ・設計は未経験
- 上流工程（要件定義・DB設計・システム設計）を重視して学びたい
- コーディングは、技術について面接で説明できるレベルまで理解したい
- CRUD・SQL・Gitは基礎を理解済み。TypeScript・React Native・非同期処理・
  認証・セキュリティ基礎は学習中
- テストコードは未経験。基礎（単体テスト）は自分で書きたい
- 一人開発だが、ブランチ・PR運用を実践し、面接で語れる経験にしたい
- CLI・コマンド操作、GitHub Actions・CI/CD、インフラ全般についても知識をつけたい
  （現時点では未学習。`docs/ai-collaboration-plan.md`の「開発後半：CI/CD」も参照）

## AIへの依頼方針（重要）

「本質的でない部分」はAIに任せてよいが、「基本となる部分・強みになる部分」は
必ず開発者と一緒に手を動かしながら実装すること。具体的には：

### 開発者と一緒に書く（AIが一方的に生成しない）
- ジオフェンス判定ロジック（アプリの核心機能）
- データベース設計・テーブル定義（`docs/schema.md` を参照。設計変更は
  必ず開発者の意図を確認してから行う）
- Supabaseのセキュリティルール（RLSポリシー）
- 認証・認可まわりのロジック（OTP検証、管理者権限チェックなど）
- 単体テストの最初の数本（書き方に慣れるまで）

### AIに任せてよい
- UIコンポーネントの見た目調整
- 定型的なCRUD処理の雛形
- テストケースの量産（パターンが定まった後）
- ドキュメント・コメントの整備

### コードを書く際の姿勢
- いきなり完成形を出さず、小さいステップで進め、都度説明を挟む
- エラーが出た場合は、直すだけでなく「なぜ起きたか」を説明する
- 面接で説明できるように、技術選定の理由も都度言語化する

### ライブラリ・パッケージ利用の判断基準（2026-08-15）
ある機能を実装する際、既存のライブラリ・パッケージを使うか、自前で実装するかは、
「どちらが開発者にとって学習上意味があるか」で判断する。
- **そのライブラリ・パッケージ自体を使う経験に学習価値がある場合**（広く使われて
  いる・面接で語れるような技術である場合）→ 積極的に使う
- **ライブラリを使わず自分で実装する方に学習価値がある場合**（仕組み・
  アルゴリズムの理解自体が目的の場合）→ 自前で実装する
どちらか判断に迷う場合は、開発者に確認してから決める。新規パッケージ追加は
引き続き事前提案・承認が必要（下記ガードレール参照）。

### ロジックコードを自分の手で書くかどうかの判断基準（2026-08-16）
「一緒に書く」対象のロジックコードであっても、実際にAIが説明して開発者が
理解するだけで終わらせず、開発者自身の手で書く時間を作るべきかどうかは、
以下で判断する。

- **既知の公式・定型的な実装をそのまま書き写すだけの部分**（例：ハーバサイン
  公式の実装）→ 手で書く価値は低い（写経に近い）。**公式自体を理解している
  ことは重要**なので説明は省略しないが、コード自体はAIが書いてよい
- **探索・設計判断を伴うアルゴリズム的な実装**（例：DFSなどのアルゴリズムを
  具体的な問題に当てはめて設計・実装する部分）→ 手を動かすことで初めて
  身につく技術的知見（文法・構文・実装パターンの引き出し）があるため、
  開発者自身が書く価値が高い。AIは説明・提案・レビュー役に回り、小さい
  単位に区切って開発者が実際に書く時間を作る
- どちらに該当するか迷う場合は、開発者に確認する

### 越えてはいけない境界（ガードレール）

実装フェーズで以下は必ず守ること。逸脱しそうな場合は、その場で止めて開発者に
確認する。

- **スコープを超えない**：依頼された単位（1タスク・1 User Story）だけを実装する。
  頼まれていない機能追加・リファクタ・「ついでの改善」は提案に留め、実装しない
- **docsと食い違う実装をしない**：`docs/schema.md`・`architecture.md`・
  `ai-collaboration-plan.md`に書かれた設計と異なる実装が必要になった場合、
  その場で判断せず、理由を説明した上で開発者の承認を得てから進める
- **理解を追い越して完成させない**：`ai-collaboration-plan.md`で🖊️指定の
  タスクは、コードを一気に書き切らない。小さい単位に区切り、開発者自身が
  書く・理解できる速度に合わせる
- **依存パッケージ・技術選定を勝手に追加しない**：新規npmパッケージの追加や、
  ADRに記録のない技術選定は、事前に提案し承認を得てから行う
- **指示なくコミット・プッシュ・PR作成をしない**：`git commit`・`git push`・
  PR作成は、明示的に依頼された時だけ行う。ただし、1つの関数・1つの修正の
  ように意味のあるまとまりが完成したタイミングでは、コミットするかどうかを
  こちらから提案してよい（毎ターン・機械的にではなく、区切りが良い時だけ）

### CLAUDE.mdの見直しタイミング
Must優先度のUser Storyを1つ完成させるごとに、このファイル全体（特に
ガードレール・仕分け方針）が実態と合っているか見直す。

### User Story単位の詳細な仕分け

上記はカテゴリレベルの大まかな方針。実装に入る前には、必ず
`docs/ai-collaboration-plan.md` で該当するUser Storyの行を確認し、
どのタスクを一緒に書き、どのタスクを任せてよいかを具体的に確認すること。
このファイルにはUser Storyごとの詳細なタスク仕分けと、そこで学ぶべき
土台概念（HTTP/API、認証・認可、セキュリティ基礎など）も対応づけてある。

## 設計チャットと実装チャットを分ける場合のルール

一人開発の運用として、「本番アプリ・DB設計を議論し `docs/` を更新するチャット」と
「実際にコードを書くチャット」を分けて進めることがある。この場合：

- `docs/`配下（`schema.md`・`architecture.md`・`decisions/`など）を、両チャットに
  共通する正（source of truth）として扱う
- 実装チャット側で、`docs/`に書かれていない設計判断（カラムの型、RLSポリシーの
  詳細など）がその場で必要になった場合の扱いは、質問の大きさで判断する：
  - **その場で1〜2往復で決まる小さい質問**（例：値のクランプ範囲、丸め方）は、
    実装チャット内でユーザーに直接確認し、確定したらそのまま`docs/`に反映してよい
    （設計チャットに持ち帰る必要はない）
  - **複数のUser Storyにまたがる話や、じっくり検討が必要な大きめの設計判断**は、
    設計チャット側に持ち帰る。待っている間、実装チャットは他のタスクを進められる
    というメリットがあるため（2026-08-15）
- 逆に設計チャット側は`docs/`の修正までを行い、コードの実装には踏み込まない
- 例外：各User Storyのブランチで作業を始められるようになるまでの
  プロジェクトセットアップ（`app/`の雛形作成など、機能に紐づかない基盤整備）は、
  開発者の指示があれば設計チャット側で`main`に直接行ってよい（2026-08-15）

## チャットとディレクトリの対応（2026-08-16）

複数チャットが並行作業するため、チャットの役割ごとに作業ディレクトリを分ける。

- **設計チャット**（`/Users/asuka/alpha/oruca`、`main`ブランチ）：アプリ・DB設計の
  相談、`docs/`の修正、User Story横断のプロジェクトセットアップ
- **実装チャット**（`/Users/asuka/alpha/oruca-worktrees/us-XXX`、各featureブランチ）：
  個別User Storyのコード実装
- **レビューチャット**（`/Users/asuka/alpha/oruca-worktrees/review`、`main`の
  detached HEAD）：各USのPRを`/code-review`でセルフレビューする専用チャット。
  実装チャットとは独立させ、実装時の思い込みを引き継がない「新鮮な目」での
  レビューを行う。レビュー対象のコードは、対象US用worktreeのファイルを読みに
  行くだけで、レビューチャット自身のブランチ切り替えは発生しない

## 共通化の方針（2026-08-16）

複数のUser Story・複数の画面で使い回せる計算処理・関数・クラスは、実装先の
User Story用ディレクトリに埋め込まず、共通の場所に切り出して使い回す。

- 例：緯度経度間の距離計算は`app/utils/geo.ts`にまとめ、US-004（ジオフェンス
  判定）・US-018（エリア登録）の両方から参照する（詳細は`docs/architecture.md`
  のディレクトリ構成を参照）
- 新しい処理を書く前に、同じような処理が既に`app/utils/`などに無いか確認する
- 該当処理が既存の「AIに任せてよい／開発者と一緒に書く」の区分（本ファイルの
  「AIへの依頼方針」章）に当てはまる場合は、共通化してもその区分は変わらない
  （ジオフェンス判定に関わる処理なら、共通化後も🖊️のまま）

## 技術スタック

| レイヤー | 技術 |
|---|---|
| フロントエンド | React Native + Expo + TypeScript |
| バックエンド／DB | Supabase（PostgreSQL） |
| 認証 | Supabase Auth |
| 地図 | react-native-maps |
| 状態管理 | Zustand（今後導入予定） |
| テスト | Jest |

選定理由の詳細は `docs/decisions/0001-tech-stack.md` を参照。

## ディレクトリ構成の方針

```
/README.md             プロジェクト概要（人間向け）
/CLAUDE.md             このファイル（AI向け）
/docs/prd.md           PRD要約版
/docs/schema.md        DB設計（ER図・テーブル定義）
/docs/architecture.md  システム構成・ライフサイクル設計
/docs/ai-collaboration-plan.md  User Story単位の役割分担
/docs/decisions/       技術選定・設計判断の記録（ADR）
/app/                  本開発用の本番アプリ（Expoプロジェクト本体）
```

`docs/`は`app/`開発時に参照する、リポジトリ直下の共有ドキュメントである。

## Git運用ルール

- 一人開発だが、機能単位・User Story単位でブランチを切る
  （例：`feature/us-018-area-registration`）
- プロジェクトのセットアップ・設定変更・typo修正のような細かい変更は、
  ブランチを切らず直接`main`にコミットしてよい（2026-08-15、ブランチ運用の
  粒度を見直し）
- ブランチ作業後は自分でPRを出し、内容を見直してからマージする
- PRタイトルにはUser Story番号を含める（例：`US-001: 在席人数のリアルタイム表示`）
- `develop`のような長期ブランチは作らない（トランクベース開発）

## 環境について（ブランチとは独立）

Supabaseプロジェクトは`demo`（合宿デモ専用・凍結）・`dev`（日常の開発用）・
`prod`（コンペ提出用の安定版）の3つを用意する。どの環境に繋ぐかは
ブランチではなく、環境変数（`.env.development` / `.env.production`）で
切り替える。詳細は`docs/decisions/0003-environments-and-branching.md`
を参照。

## User Story優先順位（MoSCoW）

詳細は `docs/prd.md` の該当セクション、または以下の要約を参照。

- **Must**: US-001, 002, 004, 005, 018（US-003はUS-001に統合）
- **Should**: US-006, 007, 008, 010, 014, 019
- **Could**: US-011, 013, 016, 017, 021
- **Won't**: US-012, 015, 020

実装順序は Must の中でも US-018 → US-004 → US-001 → US-005 → US-002 の
依存関係に従う。
